import { useEffect, useState } from 'react'
import { paper } from 'paper'

const Threedy = ({ svgData }) => {
    useEffect(() => {
        const pathData = extractPathData()
    }, [])

    
    return (
        <div class="">
            threedy
        </div>

    );
    }

export default Threedy;
